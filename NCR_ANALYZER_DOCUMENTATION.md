# NCR Analyzer - เอกสารการทำงานของระบบ

## 📋 ภาพรวมของระบบ

NCR Analyzer เป็นระบบวิเคราะห์ข้อมูล Non-Conformance Report (NCR) สำหรับแผนก PD3 โดยทำการนำเข้าข้อมูลจากไฟล์ Excel (.xlsx, .xls) หรือ CSV แล้ววิเคราะห์และจัดหมวดหมู่ปัญหาต่างๆ แสดงผลเป็นกราฟและตารางข้อมูลที่ใช้งานง่าย

---

## 🗂️ โครงสร้างข้อมูล (Data Structure)

### 1. ไฟล์ Excel ที่รองรับ
- **ชื่อไฟล์มาตรฐาน**: `NCR-PD3.xlsx`
- **Sheet ที่ใช้**: `NC รวม` (หรือ sheet แรกถ้าไม่พบ)
- **รูปแบบไฟล์**: `.xlsx`, `.xls`, `.csv`
- **ขนาดสูงสุด**: 10 MB

### 2. โครงสร้างคอลัมน์ (Column Mapping)

ระบบใช้การ mapping คอลัมน์แบบ flexible โดยค้นหาจาก:
1. **ตำแหน่งคอลัมน์** (Column Letter/Index)
2. **ชื่อคอลัมน์** (Exact Match)
3. **คำสำคัญ** (Keyword Matching)

| คอลัมน์ | ตำแหน่ง | Index | ชื่อภาษาไทย | คำสำคัญ | รายละเอียด |
|---------|---------|-------|-------------|---------|-----------|
| **H** | 7 | 7 | ตรา | `ตรา`, `brand`, `trademark` | ตราสินค้า/แบรนด์ของลูกค้า |
| **I** | 8 | 8 | ลูกค้า | `ลูกค้า`, `customer`, `client` | ชื่อบริษัทลูกค้า |
| **L** | 11 | 11 | เครื่องพิมพ์ | `เครื่องพิมพ์`, `printer`, `machine`, `เครื่อง` | เครื่องพิมพ์ที่ใช้งาน (PT1, PT2, PT3) |
| **N** | 13 | 13 | ผลการตรวจสอบ1 | `ผลการตรวจสอบ`, `ผลตรวจ`, `defect`, `problem` | รายละเอียดปัญหาที่พบ (ใช้ในการจำแนกประเภท) |
| **R** | 17 | 17 | จำนวน | `จำนวน`, `qty`, `quantity`, `amount` | จำนวนใบที่พิมพ์เสีย |

### 3. การทำงานของ Column Mapping

```javascript
// ตัวอย่างการ mapping
const columnMapping = {
    'ลูกค้า': findBestColumn(availableColumns, ['ลูกค้า', 'customer', 'client'], ['I', 8]),
    'ตรา': findBestColumn(availableColumns, ['ตรา', 'brand', 'trademark'], ['H', 7]),
    'เครื่องพิมพ์': findBestColumn(availableColumns, ['เครื่องพิมพ์', 'printer', 'machine'], ['L', 11]),
    'ผลการตรวจสอบ1': findBestColumn(availableColumns, ['ผลการตรวจสอบ', 'defect', 'problem'], ['N', 13]),
    'จำนวน': findBestColumn(availableColumns, ['จำนวน', 'qty', 'quantity'], ['R', 17])
};
```

**ลำดับการค้นหา:**
1. ค้นหาจาก `__EMPTY_X` pattern (สำหรับ Excel ที่ไม่มี header)
2. ค้นหาจาก Column Letter (H, I, L, N, R)
3. ค้นหาจากชื่อคอลัมน์แบบ exact match
4. ค้นหาจากคำสำคัญแบบ partial match

---

## 🏷️ ระบบจำแนกประเภทปัญหา (Defect Classification)

### DEFECT_RULES

ระบบใช้ Regular Expression ในการจำแนกปัญหาออกเป็น 11 หมวดหมู่:

| ลำดับ | หมวดหมู่ | คำสำคัญ | คำอธิบาย |
|------|----------|---------|----------|
| **1** | สี | สีหาย, เปื้อนสี, สีอ่อน, สีเข้ม, สีลาก, สีลงไม่เต็ม, สีไม่เท่ากัน, สีเปื่อน | ปัญหาเกี่ยวกับการพิมพ์สี |
| **2** | รอยตำหนิพิเศษจากกระบวนการ | รอยฟ้าผ่า, รอยพยาธี, รอยเส้นยาว, รอยขีดข่วน | รอยตำหนิที่เกิดจากกระบวนการผลิต |
| **3** | บล็อกเลื่อนและอาร์ตเวิร์ก | บล็อกเลื่อน, บล็อคเลื่อน, ตัวหนังสือหาย, พิมพ์ไม่ชัด, ไม่ตรงมาร์ค, อาร์ตเวิร์ก | ปัญหาการจัดวางและออกแบบ |
| **4** | โครงสร้างผ้าและวัสดุ | ผ้าพับ, ผ้าโย้, ด้ายแตก, ผ้าเป็นรู, ผ้าเป็นรอย, ผ้าเสีย | ปัญหาจากวัสดุผ้า |
| **5** | อุปกรณ์และเครื่องจักร | ฟิล์มพันลูกกลิ้ง, รอยเส้นเทป, ลายอัด, ลูกกลิ้งเป็นรอย | ปัญหาจากเครื่องจักร |
| **6** | สิ่งปนเปื้อน | แมลง, ฝุ่น, น้ำมัน, สิ่งแปลกปลอม | ปัญหาสิ่งสกปรก |
| **7** | ม้วนและงานหลังพิมพ์ | ม้วนย่น, ม้วนหลวม, ตัดเบี้ยว, ขอบไม่เรียบ | ปัญหาการม้วนและตัด |
| **8** | เทคนิค/ห้องแล็บ | Tenacity, Elongation, Denier, ไม่ได้มาตรฐาน, ไม่ผ่านเกณฑ์ | ปัญหาจากการทดสอบคุณภาพ |
| **9** | Setup และบุคคล | ขึ้นผ้าผิด, สีผิด, บล็อกผิด, บล็อคผิด, กลับด้าน | ปัญหาจากการตั้งค่า |
| **10** | ฉลากและข้อมูล | ป้ายผิด, Lot ไม่ตรง, ชื่อลูกค้าผิด, รหัสผิด | ปัญหาข้อมูลและฉลาก |
| **11** | ขนส่งและจัดเก็บ | งานตีกลับ, ม้วนกระแทก, แจ้งขนส่ง, พาเลทชำรุด | ปัญหาการจัดส่ง |

### ตัวอย่าง DEFECT_RULES

```javascript
const DEFECT_RULES = {
    "1. สี": /สีหาย|เปื้อนสี|สีอ่อน|สีเข้ม|สีลาก|สีลงไม่เต็ม|สีไม่เท่ากัน|สีเปื่อน/i,
    "2. รอยตำหนิพิเศษจากกระบวนการ": /รอยฟ้าผ่า|รอยพยาธี|รอยเส้นยาว|รอยขีดข่วน/i,
    "3. บล็อกเลื่อนและอาร์ตเวิร์ก": /บล็อกเลื่อน|บล็อคเลื่อน|ตัวหนังสือหาย|พิมพ์ไม่ชัด|ไม่ตรงมาร์ค|อาร์ตเวิร์ก/i,
    // ... และอื่นๆ
};
```

### การจำแนกแบบอัตโนมัติ

```javascript
function classifyDefect(text) {
    if (!text || text.toString().trim() === '') {
        return "ไม่ระบุสาเหตุ";
    }

    const textStr = text.toString().trim();

    // วนลูปเช็คตาม DEFECT_RULES
    for (const [groupName, pattern] of Object.entries(DEFECT_RULES)) {
        if (pattern.test(textStr)) {
            return groupName;
        }
    }

    return "อื่นๆ / พิมพ์เสียทั่วไป";
}
```

**หลักการทำงาน:**
1. รับข้อความจากคอลัมน์ "ผลการตรวจสอบ1" (คอลัมน์ N)
2. วนลูปตรวจสอบกับ DEFECT_RULES ทีละหมวด
3. ถ้าตรงกับ pattern ใด จะคืนค่าชื่อหมวดนั้น
4. ถ้าไม่ตรงเลย จะคืนค่า "อื่นๆ / พิมพ์เสียทั่วไป"

---

## ⚙️ กระบวนการประมวลผล (Data Processing)

### 1. การอ่านไฟล์ (File Reading)

```javascript
function readExcelFile(file) {
    // อ่านไฟล์ Excel ด้วย SheetJS (xlsx library)
    const workbook = XLSX.read(data, { 
        type: 'binary',
        cellDates: true,
        raw: false
    });
    
    // ค้นหา Sheet "NC รวม" หรือใช้ sheet แรก
    let sheetName = workbook.SheetNames.find(name => 
        name.includes('NC') && name.includes('รวม')
    ) || workbook.SheetNames[0];
    
    // แปลงเป็น JSON
    const jsonData = XLSX.utils.sheet_to_json(worksheet, {
        defval: '',
        blankrows: false,
        raw: false
    });
    
    return jsonData;
}
```

### 2. การ Mapping คอลัมน์

```javascript
// ค้นหาคอลัมน์ที่เหมาะสม
const columnMapping = {
    'ลูกค้า': findBestColumn(availableColumns, ['ลูกค้า', 'customer'], ['I', 8]),
    'ตรา': findBestColumn(availableColumns, ['ตรา', 'brand'], ['H', 7]),
    // ... mapping อื่นๆ
};
```

### 3. การประมวลผลข้อมูล

```javascript
const processed = inputData.map((row, index) => {
    // อ่านข้อมูลจากแต่ละคอลัมน์
    const customer = (row[columnMapping['ลูกค้า']] || '').toString().trim();
    const brand = (row[columnMapping['ตรา']] || '').toString().trim();
    const printer = (row[columnMapping['เครื่องพิมพ์']] || '').toString().trim();
    const defectDescription = (row[columnMapping['ผลการตรวจสอบ1']] || '').toString().trim();
    
    // แปลงค่าจำนวนเป็นตัวเลข
    let quantity = 0;
    const qtyValue = row[columnMapping['จำนวน']];
    if (qtyValue !== undefined && qtyValue !== null && qtyValue !== '') {
        if (typeof qtyValue === 'number') {
            quantity = qtyValue;
        } else {
            quantity = parseFloat(qtyValue.toString().replace(/[^0-9.-]/g, '')) || 0;
        }
    }

    return {
        customer,
        brand,
        printer,
        defectDescription,
        quantity,
        defectGroup: classifyDefect(defectDescription), // จำแนกประเภทอัตโนมัติ
        rowIndex: index + 1
    };
}).filter(row => {
    // กรองเฉพาะแถวที่มีข้อมูลครบถ้วน (ต้องมีลูกค้า และ ตรา)
    return row.customer && row.customer.length > 0 && 
           row.brand && row.brand.length > 0;
});
```

### 4. การจัดกลุ่มข้อมูล (Data Grouping)

```javascript
// จัดกลุ่มตาม: ลูกค้า + ตราสินค้า + หมวดหมู่ปัญหา
const summary = {};
processed.forEach(row => {
    const key = `${row.customer}|${row.brand}|${row.defectGroup}`;
    if (!summary[key]) {
        summary[key] = {
            customer: row.customer,
            brand: row.brand,
            defectGroup: row.defectGroup,
            totalQuantity: 0,      // ยอดรวมทั้งหมด
            occurrences: 0         // จำนวนครั้งที่พบ
        };
    }
    summary[key].totalQuantity += row.quantity;
    summary[key].occurrences += 1;
});
```

---

## 📊 การแสดงผล (Data Visualization)

### 1. Summary Cards (การ์ดสรุป)

แสดง 4 ค่าสำคัญ:
- **จำนวนลูกค้า**: นับจำนวน customer ที่ไม่ซ้ำกัน
- **จำนวนตราสินค้า**: นับจำนวน brand ที่ไม่ซ้ำกัน
- **ประเภทปัญหา**: นับจำนวน defectGroup ที่ไม่ซ้ำกัน
- **ยอดพิมพ์เสียรวม**: รวม totalQuantity ทั้งหมด

### 2. กราฟ (Charts)

| กราฟ | ประเภท | รายละเอียด |
|------|--------|-----------|
| **Top 10 ปัญหาที่พบบ่อย** | Bar Chart | แสดง 10 หมวดปัญหาที่มียอดสูงสุด |
| **การกระจายตามลูกค้า** | Doughnut Chart | แสดง Top 8 ลูกค้าที่มียอดพิมพ์เสียสูงสุด |
| **ทุกประเภทปัญหา** | Horizontal Bar | แสดงทุกหมวดปัญหาเรียงตามมูลค่า |
| **เปรียบเทียบลูกค้า** | Grouped Bar | เปรียบเทียบยอดแต่ละลูกค้า |
| **เปรียบเทียบตราสินค้า** | Grouped Bar | เปรียบเทียบยอดแต่ละตราสินค้า |

### 3. ตารางข้อมูล (Data Table)

**คอลัมน์ที่แสดง:**
- ลำดับ
- ลูกค้า
- ตราสินค้า
- หมวดหมู่ปัญหา
- ยอดพิมพ์เสีย (ใบ)
- จำนวนครั้งที่พบ
- เฉลี่ยต่อครั้ง (ใบ)
- ดูรายละเอียด (ปุ่ม)

**Features:**
- Search (ค้นหา)
- Sort (เรียงลำดับ)
- Pagination (แบ่งหน้า - 10 รายการ/หน้า)
- Click เพื่อดูรายละเอียด

### 4. Modal รายละเอียด (Detail Modal)

เมื่อคลิก "ดูรายละเอียด" จะแสดง:

**ส่วนที่ 1: สรุปข้อมูล**
- ลูกค้า
- ตราสินค้า
- หมวดหมู่ปัญหา
- ยอดพิมพ์เสียรวม
- จำนวนครั้งที่พบ
- เฉลี่ยต่อครั้ง

**ส่วนที่ 2: สรุปตามเครื่องพิมพ์**
- แสดง Card แต่ละเครื่อง (PT1, PT2, PT3)
- จำนวนใบที่พิมพ์เสีย
- จำนวนครั้งที่พบ
- เรียงจากยอดมากไปน้อย

**ส่วนที่ 3: รายละเอียดแต่ละครั้ง**
- ลำดับ
- จำนวนใบ
- รายละเอียดปัญหาเต็ม

---

## 🔍 การตรวจสอบความถูกต้อง (Data Validation)

### 1. การกรองข้อมูล

```javascript
// กรองเฉพาะแถวที่มีข้อมูลครบถ้วน
.filter(row => {
    const hasCustomer = row.customer && row.customer.length > 0;
    const hasBrand = row.brand && row.brand.length > 0;
    return hasCustomer && hasBrand;
});
```

**เงื่อนไข:**
- ต้องมีข้อมูล "ลูกค้า" (customer)
- ต้องมีข้อมูล "ตราสินค้า" (brand)
- ถ้าไม่มีอย่างใดอย่างหนึ่ง จะถูกกรองออก

### 2. การจัดการข้อมูลขาดหาย

| Field | ค่า Default | การจัดการ |
|-------|-------------|-----------|
| customer | - | **ต้องมี** (ไม่มีจะถูกกรอง) |
| brand | - | **ต้องมี** (ไม่มีจะถูกกรอง) |
| printer | 'ไม่ระบุเครื่องพิมพ์' | ใช้ค่า default |
| defectDescription | '' | ใช้ค่าว่าง |
| quantity | 0 | แปลงเป็น 0 |

### 3. การแปลงข้อมูล

```javascript
// แปลง quantity จาก string เป็น number
let quantity = 0;
if (typeof qtyValue === 'number') {
    quantity = qtyValue;
} else {
    // ลบอักขระพิเศษออก แล้วแปลงเป็นตัวเลข
    quantity = parseFloat(qtyValue.toString().replace(/[^0-9.-]/g, '')) || 0;
}
```

---

## 📈 สถิติและการคำนวณ

### 1. ยอดรวม (Total)
```javascript
totalQuantity = Σ(quantity) // รวมจำนวนใบทั้งหมด
```

### 2. จำนวนครั้ง (Occurrences)
```javascript
occurrences = count(records) // นับจำนวนรายการ
```

### 3. ค่าเฉลี่ย (Average)
```javascript
average = totalQuantity / occurrences
```

### 4. การจัดกลุ่มตามเครื่องพิมพ์
```javascript
// สรุปยอดตามเครื่องพิมพ์
const printerStats = {};
relatedData.forEach(item => {
    const printerName = item.printer || 'ไม่ระบุเครื่องพิมพ์';
    if (!printerStats[printerName]) {
        printerStats[printerName] = {
            totalQty: 0,
            count: 0
        };
    }
    printerStats[printerName].totalQty += item.quantity;
    printerStats[printerName].count += 1;
});

// เรียงตามยอดมากไปน้อย
const sortedPrinters = Object.entries(printerStats)
    .sort(([,a], [,b]) => b.totalQty - a.totalQty);
```

---

## 🛠️ การใช้งาน (Usage)

### ขั้นตอนการใช้งาน

1. **อัปโหลดไฟล์**
   - คลิกที่พื้นที่อัปโหลด หรือ Drag & Drop
   - เลือกไฟล์ `.xlsx`, `.xls`, หรือ `.csv`
   - ขนาดไฟล์ไม่เกิน 10 MB

2. **วิเคราะห์ข้อมูล**
   - คลิกปุ่ม "เริ่มการวิเคราะห์"
   - ระบบจะประมวลผลอัตโนมัติ
   - รอ 2-3 วินาที

3. **ดูผลลัพธ์**
   - ดูสรุปจากการ์ดด้านบน
   - ดูกราฟแต่ละประเภท
   - ค้นหาข้อมูลในตาราง
   - คลิก "ดูรายละเอียด" เพื่อดูข้อมูลเชิงลึก

### การส่งออกข้อมูล

- **พิมพ์สรุปครื่องพิมพ์**: คลิกปุ่ม 🖨️ ที่ Modal รายละเอียด
- **ดาวน์โหลดข้อมูล**: (Feature ที่จะเพิ่มในอนาคต)

---

## 🔧 การปรับแต่ง (Customization)

### เพิ่มหมวดหมู่ปัญหาใหม่

แก้ไข `DEFECT_RULES` ในไฟล์ `NCR_analyzer.html`:

```javascript
const DEFECT_RULES = {
    // หมวดเดิม...
    "12. หมวดใหม่": /คำสำคัญ1|คำสำคัญ2|คำสำคัญ3/i,
};
```

**หมายเหตุ:**
- ใช้ `|` เพื่อแยกคำสำคัญ
- ใช้ `/i` ต่อท้ายเพื่อไม่สนใจตัวพิมพ์เล็ก-ใหญ่
- เพิ่มคำสำคัญให้ครบถ้วนเพื่อความแม่นยำ

### เปลี่ยน Column Mapping

แก้ไข `columnMapping` ถ้าโครงสร้างไฟล์เปลี่ยน:

```javascript
const columnMapping = {
    'ลูกค้า': findBestColumn(availableColumns, ['ลูกค้า', 'customer'], ['I', 8]),
    // เปลี่ยนตำแหน่งคอลัมน์ หรือเพิ่มคำสำคัญ
};
```

---

## ❗ ข้อควรระวัง (Important Notes)

1. **คอลัมน์ที่จำเป็น**: ต้องมี "ลูกค้า" และ "ตรา" เสมอ
2. **จำนวน (R)**: ต้องเป็นตัวเลข หรือแปลงเป็นตัวเลขได้
3. **ผลการตรวจสอบ (N)**: ใช้ข้อความ (text) ในการจำแนก
4. **เครื่องพิมพ์ (L)**: ถ้าไม่มีจะแสดงเป็น "ไม่ระบุเครื่องพิมพ์"
5. **ชื่อ Sheet**: ควรตั้งชื่อเป็น "NC รวม" หรือระบุไว้ใน sheet แรก

---

## 📝 ตัวอย่างข้อมูล (Sample Data)

### โครงสร้างไฟล์ Excel

| A | B | C | ... | H (ตรา) | I (ลูกค้า) | ... | L (เครื่อง) | ... | N (ผลตรวจสอบ) | ... | R (จำนวน) |
|---|---|---|-----|---------|-----------|-----|------------|-----|---------------|-----|-----------|
| 1 | ... | ... | ... | Zhejiang UN | บริษัท ที.ซี.ยูเนียน... | ... | PT3 | ... | พบเปื้อนสีด้านหน้า... | ... | 564 |
| 2 | ... | ... | ... | Zhejiang UN | บริษัท ที.ซี.ยูเนียน... | ... | PT1 | ... | สีหาย... | ... | 1200 |

### ผลลัพธ์ที่ได้

```json
{
  "customer": "บริษัท ที.ซี.ยูเนียน อะโกรเทค จำกัด",
  "brand": "Zhejiang UN",
  "printer": "PT3",
  "defectDescription": "พบเปื้อนสีด้านหน้าและด้านหลัง...",
  "quantity": 564,
  "defectGroup": "1. สี",
  "rowIndex": 1
}
```

---