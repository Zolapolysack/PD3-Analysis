# 🚀 คู่มือการ Deploy บน GitHub Pages

## ขั้นตอนที่ 1: สร้าง Repository บน GitHub

1. เข้า https://github.com/new
2. ตั้งชื่อ Repository เช่น `pd3-analysis` หรือ `pd3-analytics-hub`
3. ตั้งค่าเป็น **Public** (จำเป็นสำหรับ GitHub Pages แบบฟรี)
4. **ไม่ต้อง** เลือก "Initialize with README" (เพราะเรามีแล้ว)
5. คลิก "Create repository"

## ขั้นตอนที่ 2: Push โค้ดขึ้น GitHub

คัดลอกและรันคำสั่งเหล่านี้ใน PowerShell:

```powershell
cd "C:\Users\Zola Polysack\Desktop\PD3-Analysis"

# เพิ่ม remote repository (แทนที่ YOUR_USERNAME ด้วยชื่อ GitHub ของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Push ขึ้น GitHub
git push -u origin main
```

**ตัวอย่าง:**
```powershell
git remote add origin https://github.com/awirut/pd3-analysis.git
git push -u origin main
```

## ขั้นตอนที่ 3: เปิดใช้งาน GitHub Pages

1. ไปที่ Repository บน GitHub
2. คลิก **Settings** (ตรงแถบบน)
3. เลือก **Pages** (เมนูซ้าย)
4. ในส่วน **Source**:
   - เลือก Branch: `main`
   - เลือก Folder: `/ (root)`
5. คลิก **Save**
6. รอสักครู่ (1-2 นาที) GitHub จะ deploy ให้

## ขั้นตอนที่ 4: เข้าถึงเว็บไซต์

เว็บไซต์ของคุณจะอยู่ที่:
```
https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/
```

**ตัวอย่าง:**
```
https://awirut.github.io/pd3-analysis/
```

---

## 📋 Checklist ก่อน Push

- [x] Git repository ถูกสร้างแล้ว (`git init`)
- [x] ไฟล์ทั้ง 3 พร้อมแล้ว (index.html, NCR_analyzer.html, PD3_analyzer_fullyear.html)
- [x] README.md ถูกสร้างแล้ว
- [x] .gitignore ถูกสร้างแล้ว
- [x] Commit แรกเสร็จแล้ว
- [x] Branch main พร้อมแล้ว
- [ ] สร้าง Repository บน GitHub
- [ ] เพิ่ม remote origin
- [ ] Push ขึ้น GitHub
- [ ] เปิดใช้งาน GitHub Pages

---

## 🔧 แก้ไขปัญหาที่พบบ่อย

### ปัญหา: Authentication ล้มเหลว
**วิธีแก้:** ใช้ Personal Access Token แทนรหัสผ่าน
1. ไป https://github.com/settings/tokens
2. Generate new token (classic)
3. เลือก repo scope
4. คัดลอก token
5. ใช้ token แทนรหัสผ่านเมื่อ push

### ปัญหา: Remote origin already exists
**วิธีแก้:**
```powershell
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
```

### ปัญหา: หน้าเว็บแสดง 404
**วิธีแก้:**
1. ตรวจสอบว่า index.html อยู่ที่ root
2. รอ 1-2 นาที หลังเปิด GitHub Pages
3. Clear cache เบราว์เซอร์ (Ctrl+Shift+R)

---

## 🔄 การอัพเดทเว็บไซต์

เมื่อแก้ไขโค้ดแล้วต้องการอัพเดท:

```powershell
cd "C:\Users\Zola Polysack\Desktop\PD3-Analysis"
git add .
git commit -m "Update: คำอธิบายการเปลี่ยนแปลง"
git push
```

GitHub Pages จะอัพเดทอัตโนมัติภายใน 1-2 นาที

---

## 📞 ติดต่อ

พัฒนาโดย: **Awirut (ZP1048)**
Version: **2.0**
